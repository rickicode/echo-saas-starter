# echo-saas-starter
Go + Echo + TanStack SPA starter kit with plugin modular architecture. Auth (PASETO), RBAC, subscription plans, user management, docs CMS, admin + user dashboards. Dual DB: PostgreSQL + SQLite3 WAL.
